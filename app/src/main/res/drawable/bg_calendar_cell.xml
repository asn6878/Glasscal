<?xml version="1.0" encoding="utf-8"?>
<!-- Enhanced Calendar Cell with Glassmorphism Effect -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Shadow Layer (Blue Shadow #004585 with 20% opacity, 15dp blur simulation) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/glass_shadow" />
            <corners android:radius="24dp" />
        </shape>
    </item>

    <!-- Glass Background Layer with Gradient (White 50% -> 20%) -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:angle="270"
                android:startColor="#80FFFFFF"
                android:endColor="#33FFFFFF"
                android:type="linear" />
            <corners android:radius="24dp" />
            <stroke
                android:width="1.5dp"
                android:color="@color/glass_border_bright" />
        </shape>
    </item>

</layer-list>
