<?xml version="1.0" encoding="utf-8"?>
<!-- Enhanced Glassmorphism Card Background -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Shadow layer (Blue Shadow #004585 with 20% opacity, 15dp blur simulation) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/glass_shadow" />
            <corners android:radius="24dp" />
        </shape>
    </item>

    <!-- Main glass background with gradient (White 60% -> 0%) -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:angle="270"
                android:startColor="@color/glass_gradient_start"
                android:endColor="@color/glass_gradient_end"
                android:type="linear" />
            <corners android:radius="24dp" />
            <stroke
                android:width="1dp"
                android:color="@color/glass_border" />
        </shape>
    </item>

</layer-list>
